package assets.combo
{
   public class ComboOverButton extends ComboButtonState
   {
      public function ComboOverButton()
      {
         super();
         bmpLeft = new combo_OVER_LEFT(1,1);
         bmpCenter = new combo_OVER_CENTER(1,1);
         bmpRight = new combo_OVER_RIGHT(1,1);
      }
   }
}

