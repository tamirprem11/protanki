package forms.buttons
{
   import flash.display.Bitmap;
   
   public class §'!]§ extends MainPanelWideButton
   {
      private static const iconN:Class = §do const while§;
      
      private static const overBtn:Class = §0a§;
      
      private static const normalBtn:Class = §dynamic for for§;
      
      public function §'!]§()
      {
         super(new Bitmap(new iconN().bitmapData),5,5,new Bitmap(new overBtn().bitmapData),new Bitmap(new normalBtn().bitmapData));
      }
   }
}

