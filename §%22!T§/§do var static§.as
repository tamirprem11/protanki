package §"!T§
{
   import §finally for else§.§case include§;
   import projects.tanks.client.panel.model.dailyquest.§72§;
   import projects.tanks.client.panel.model.dailyquest.§;!§;
   
   public class §do var static§ extends §;!§ implements §72§, §super for final§
   {
      public function §do var static§()
      {
         super();
      }
      
      public function skipDailyQuest(param1:§case include§) : void
      {
         if(param1.canSkipForFree)
         {
            server.§import var const§(param1.questId);
         }
         else
         {
            server.§extends for throw§(param1.questId,param1.skipCost);
         }
      }
      
      public function takePrize(param1:§case include§) : void
      {
         server.takePrize(param1.questId);
      }
   }
}

