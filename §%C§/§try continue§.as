package §%C§
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §try continue§
   {
      private static const §break static§:Class = §class do§;
      
      public static const §="9§:BitmapData = Bitmap(new §break static§()).bitmapData;
      
      private static const §import for with§:Class = get;
      
      public static const §9"P§:BitmapData = Bitmap(new §import for with§()).bitmapData;
      
      private static const §function for super§:Class = §const const case§;
      
      public static const §4!=§:BitmapData = Bitmap(new §function for super§()).bitmapData;
      
      private static const §default const continue§:Class = §+"T§;
      
      public static const §2!%§:BitmapData = Bitmap(new §default const continue§()).bitmapData;
      
      private static const §var var with§:Class = §return for extends§;
      
      public static const §!"Q§:BitmapData = Bitmap(new §var var with§()).bitmapData;
      
      private static const §0A§:Class = §>!&§;
      
      public static const §while for return§:BitmapData = Bitmap(new §0A§()).bitmapData;
      
      private static const §8!d§:Class = §true set§;
      
      public static const §return const import§:BitmapData = Bitmap(new §8!d§()).bitmapData;
      
      private static const §-"?§:Class = §4!`§;
      
      public static const §false for implements§:BitmapData = Bitmap(new §-"?§()).bitmapData;
      
      private static const §?!c§:Class = §<^§;
      
      public static const §else do§:BitmapData = Bitmap(new §?!c§()).bitmapData;
      
      public function §try continue§()
      {
         super();
      }
   }
}

