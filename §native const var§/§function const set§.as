package §native const var§
{
   import §8=§.MessageColor;
   import §],§.§while var const§;
   import alternativa.tanks.locale.constants.TextConst;
   import flash.utils.Dictionary;
   
   public class §function const set§
   {
      public static const §<!f§:§return native§ = new §return native§(MessageColor.§false const try§,TextConst.TEXT_DOM_WE_CAPTURED_POINT);
      
      public static const §use var null§:§return native§ = new §return native§(MessageColor.§each var use§,TextConst.TEXT_DOM_ENEMIES_CAPTURED_POINT);
      
      public static const §include set final§:§return native§ = new §return native§(MessageColor.§each var use§,TextConst.TEXT_DOM_WE_LOST_POINT);
      
      public static const §,"K§:§return native§ = new §return native§(MessageColor.§false const try§,TextConst.TEXT_DOM_ENEMIES_LOST_POINT);
      
      private static const §case for use§:§return native§ = new §return native§(MessageColor.BLUE,TextConst.TEXT_DOM_BLUE_CAPTURED_POINT);
      
      private static const §include for if§:§return native§ = new §return native§(MessageColor.RED,TextConst.TEXT_DOM_RED_CAPTURED_POINT);
      
      private static const §final var import§:Dictionary = new Dictionary();
      
      private static const §var for false§:§return native§ = new §return native§(MessageColor.BLUE,TextConst.TEXT_DOM_BLUE_LOST_POINT);
      
      private static const §'T§:§return native§ = new §return native§(MessageColor.RED,TextConst.TEXT_DOM_RED_LOST_POINT);
      
      private static const §import const for§:Dictionary = new Dictionary();
      
      §final var import§[§while var const§.BLUE] = §case for use§;
      §final var import§[§while var const§.RED] = §include for if§;
      §import const for§[§while var const§.BLUE] = §var for false§;
      §import const for§[§while var const§.RED] = §'T§;
      
      public function §function const set§()
      {
         super();
      }
      
      public static function §continue var var§(param1:§while var const§) : §return native§
      {
         return §final var import§[param1];
      }
      
      public static function §null var in§(param1:§while var const§) : §return native§
      {
         return §import const for§[param1];
      }
   }
}

