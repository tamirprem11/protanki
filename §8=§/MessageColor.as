package §8=§
{
   import §],§.§while var const§;
   
   public class MessageColor
   {
      public static const §false const try§:uint = 65280;
      
      public static const §each var use§:uint = 16776960;
      
      public static const BLUE:uint = 4691967;
      
      public static const RED:uint = 15741974;
      
      public static const DEFAULT:uint = 65280;
      
      public static const YELLOW:uint = 16776960;
      
      public static const §13§:uint = 16750081;
      
      public static const WHITE:uint = 14540253;
      
      public function MessageColor()
      {
         super();
      }
      
      public static function § "$§(param1:§while var const§) : uint
      {
         switch(param1)
         {
            case §while var const§.BLUE:
               return MessageColor.BLUE;
            case §while var const§.RED:
               return MessageColor.RED;
            default:
               return MessageColor.DEFAULT;
         }
      }
      
      public static function §false for package§(param1:§while var const§, param2:Boolean) : uint
      {
         if(param2)
         {
            return MessageColor.YELLOW;
         }
         switch(param1)
         {
            case §while var const§.BLUE:
               return MessageColor.BLUE;
            case §while var const§.RED:
               return MessageColor.RED;
            default:
               return MessageColor.DEFAULT;
         }
      }
      
      public static function §override const false§(param1:§while var const§, param2:Boolean, param3:Boolean) : uint
      {
         if(!param2)
         {
            return MessageColor.WHITE;
         }
         if(param3)
         {
            return 16777071;
         }
         switch(param1)
         {
            case §while var const§.BLUE:
               return 10013694;
            case §while var const§.RED:
               return 16685208;
            default:
               return MessageColor.DEFAULT;
         }
      }
      
      public static function §!!!§(param1:uint) : Boolean
      {
         return param1 == MessageColor.BLUE || param1 == MessageColor.RED;
      }
   }
}

