package §class const import§
{
   import platform.client.fp10.core.model.impl.Model;
   import platform.client.fp10.core.type.IGameObject;
   
   public class §`"Z§ implements §with const continue§
   {
      private var object:IGameObject;
      
      private var impl:§with const continue§;
      
      public function §`"Z§(param1:IGameObject, param2:§with const continue§)
      {
         super();
         this.object = param1;
         this.impl = param2;
      }
      
      public function §=W§(param1:String) : void
      {
         var §8!V§:String = param1;
         var §include const implements§:String = §8!V§;
         try
         {
            Model.object = this.object;
            this.impl.§=W§(§include const implements§);
         }
         finally
         {
            Model.popObject();
         }
      }
   }
}

