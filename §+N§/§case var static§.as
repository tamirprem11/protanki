package §+N§
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §case var static§
   {
      private static const §default const continue§:Class = §-"[§;
      
      private static const §7!0§:BitmapData = Bitmap(new §default const continue§()).bitmapData;
      
      private static const §get catch§:Class = §+"E§;
      
      private static const §static set do§:BitmapData = Bitmap(new §get catch§()).bitmapData;
      
      private static const §="c§:Class = §continue import§;
      
      private static const §+"L§:BitmapData = Bitmap(new §="c§()).bitmapData;
      
      private static const §4"<§:Class = §@0§;
      
      private static const §true var finally§:BitmapData = Bitmap(new §4"<§()).bitmapData;
      
      private static const §finally const const§:Class = §#"d§;
      
      private static const §["Z§:BitmapData = Bitmap(new §finally const const§()).bitmapData;
      
      private static const §]N§:Class = §case const case§;
      
      private static const §>"K§:BitmapData = Bitmap(new §]N§()).bitmapData;
      
      private static const §super var do§:Class = §]-§;
      
      private static const §6M§:BitmapData = Bitmap(new §super var do§()).bitmapData;
      
      private static const §,!M§:Class = §8>§;
      
      private static const §const const include§:BitmapData = Bitmap(new §,!M§()).bitmapData;
      
      private static const §0!U§:Class = §4"A§;
      
      private static const §&!'§:BitmapData = Bitmap(new §0!U§()).bitmapData;
      
      private static const §&M§:Class = §try const while§;
      
      private static const §,F§:BitmapData = Bitmap(new §&M§()).bitmapData;
      
      private static const § '§:Class = §<X§;
      
      private static const §include const static§:BitmapData = Bitmap(new § '§()).bitmapData;
      
      private static const §3!5§:Class = §^!f§;
      
      private static const §"@§:BitmapData = Bitmap(new §3!5§()).bitmapData;
      
      public function §case var static§()
      {
         super();
      }
      
      public static function §class var while§(param1:Boolean, param2:Boolean) : BitmapData
      {
         return param1 ? (param2 ? §+"L§ : §7!0§) : (param2 ? §true var finally§ : §static set do§);
      }
      
      public static function §final var do§(param1:Boolean, param2:Boolean) : BitmapData
      {
         return param1 ? (param2 ? §6M§ : §["Z§) : (param2 ? §const const include§ : §>"K§);
      }
      
      public static function §do const dynamic§(param1:Boolean, param2:Boolean) : BitmapData
      {
         return param1 ? (param2 ? §include const static§ : §&!'§) : (param2 ? §"@§ : §,F§);
      }
   }
}

