package §true for package§
{
   public class §5"b§
   {
      private var §use set break§:Number;
      
      private var §while false§:Number;
      
      private var §case for for§:Number;
      
      private var §package set function§:Number;
      
      public function §5"b§(param1:Number, param2:Number, param3:Number)
      {
         super();
         this.§use set break§ = param1;
         this.§while false§ = param2;
         this.§case for for§ = param3;
         this.§package set function§ = param2 - param1;
      }
      
      public function §3;§(param1:Number) : Number
      {
         if(this.§package set function§ <= 0)
         {
            return 1;
         }
         if(param1 <= this.§use set break§)
         {
            return 1;
         }
         if(param1 >= this.§while false§)
         {
            return 0.01 * this.§case for for§;
         }
         return 0.01 * (this.§case for for§ + (this.§while false§ - param1) * (100 - this.§case for for§) / this.§package set function§);
      }
      
      public function §&a§() : Number
      {
         return this.§use set break§;
      }
      
      public function §6u§() : Number
      {
         return this.§while false§;
      }
   }
}

